# JavaScript Questions

## Fundamentals

1. What are the different data types in JavaScript?
2. Explain the difference between `var`, `let`, and `const`.
3. What is hoisting in JavaScript?
4. Explain the concept of scope (global, function, block).
5. What is the Temporal Dead Zone (TDZ)?
6. What is the difference between `==` and `===`?
7. Explain type coercion in JavaScript.
8. What are truthy and falsy values?

## Functions

9. What is the difference between function declarations and function expressions?
10. Explain arrow functions and how they differ from regular functions.
11. What are higher-order functions? Give examples.
12. Explain `call()`, `apply()`, and `bind()` methods.
13. What is a callback function?
14. What are pure functions?
15. Explain IIFE (Immediately Invoked Function Expression).
16. What is function currying?

## Objects & Prototypes

17. How do you create objects in JavaScript?
18. Explain prototypal inheritance.
19. What is the prototype chain?
20. What is the difference between `Object.create()` and constructor functions?
21. Explain `this` keyword in different contexts.
22. What are getters and setters?
23. How does `Object.freeze()` differ from `Object.seal()`?

## Arrays

24. What are the different ways to iterate over an array?
25. Explain `map()`, `filter()`, `reduce()`, and `forEach()`.
26. What is the difference between `slice()` and `splice()`?
27. How do you remove duplicates from an array?
28. Explain `find()` vs `filter()`.
29. What is array destructuring?

## Asynchronous JavaScript

30. What is the event loop?
31. Explain the call stack and callback queue.
32. What are Promises? Explain their states.
33. What is Promise chaining?
34. Explain `async/await` syntax.
35. What is the difference between `Promise.all()`, `Promise.race()`, `Promise.allSettled()`, and `Promise.any()`?
36. How do you handle errors in Promises and async/await?
37. What are microtasks and macrotasks?

## Closures & Scope

38. What is a closure? Give a practical example.
39. How do closures help in data privacy?
40. Explain lexical scoping.
41. What is the module pattern?

## ES6+ Features

42. Explain spread and rest operators.
43. What are template literals?
44. Explain destructuring (arrays and objects).
45. What are default parameters?
46. Explain `Map` and `Set` data structures.
47. What are Symbols and when would you use them?
48. Explain generators and iterators.
49. What are Proxy and Reflect?

## DOM & Events

50. What is event delegation?
51. Explain event bubbling and capturing.
52. What is `event.preventDefault()` vs `event.stopPropagation()`?
53. What is the difference between `innerHTML` and `textContent`?
54. Explain `DOMContentLoaded` vs `load` event.

## Memory & Performance

55. What is garbage collection in JavaScript?
56. What are memory leaks and how do you prevent them?
57. Explain debouncing and throttling.
58. What is memoization?

## Error Handling

59. Explain `try...catch...finally`.
60. What are custom errors?
61. What is the difference between throw and return?

## Miscellaneous

62. What is `"use strict"` mode?
63. Explain `null` vs `undefined`.
64. What is `NaN` and how do you check for it?
65. What is the difference between shallow copy and deep copy?
66. Explain `JSON.stringify()` and `JSON.parse()`.
67. What are Web APIs?
68. Explain localStorage, sessionStorage, and cookies.
69. What is the Same-Origin Policy?
70. Explain CORS (Cross-Origin Resource Sharing).

## Coding Challenges

71. Implement a debounce function.
72. Implement a throttle function.
73. Write a function to flatten a nested array.
74. Implement `Promise.all()` from scratch.
75. Write a function to deep clone an object.
76. Implement a simple pub/sub pattern.
77. Write a function to check if two objects are deeply equal.
78. Implement Function.prototype.bind from scratch.
79. Write a memoization function.
80. Implement a simple event emitter class.
