# Next.js Routing

# 📚 Navigation

- [Beginner](#-beginner)
- [Intermediate](#-intermediate)
- [Advanced](#-advanced)

---

## 🟢 Beginner

### 1. File-Based Routing

**Question:** How does Next.js App Router's file-based routing work? Explain the role of `page.tsx`, `layout.tsx`, `loading.tsx`, `error.tsx`, and `not-found.tsx`.

### 2. Dynamic Routes

**Question:** How do you create dynamic routes with `[slug]` and catch-all routes with `[...slug]`? When do you use `generateStaticParams`?

---

## 🟡 Intermediate

### 1. Route Groups & Parallel Routes

**Question:** What are route groups `(group)` and parallel routes `@slot`? Design a dashboard layout with a sidebar, main content, and modal that can be independently loaded.

### 2. Middleware

**Question:** How does `middleware.ts` work in Next.js? Write middleware that handles authentication, redirects, and geo-based routing.

---

## 🔴 Advanced

### 1. Intercepting Routes

**Question:** What are intercepting routes (`(.)`, `(..)`, `(...)`)? How do you use them to show a modal when clicking a link but a full page when navigating directly?

### 2. Server Actions

**Question:** What are Server Actions in Next.js? How do they replace API routes for mutations? Show a form that creates a database record using a Server Action with optimistic updates.
