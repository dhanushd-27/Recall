# Python Advanced Topics

# 📚 Navigation

- [Beginner](#-beginner)
- [Intermediate](#-intermediate)
- [Advanced](#-advanced)

---

## 🟢 Beginner

### 1. Classes & OOP

**Question:** How does Python OOP compare to JavaScript classes? Explain `__init__`, `self`, inheritance, and `@dataclass`.

### 2. Exception Handling

## **Question:** How does Python's `try/except/finally/else` differ from JavaScript's `try/catch/finally`? Show custom exception classes and best practices.

## 🟡 Intermediate

### 1. Context Managers

**Question:** What is `with` statement and how do you write a custom context manager? Show both class-based (`__enter__`/`__exit__`) and `@contextmanager` decorator approaches.

### 2. FastAPI

## **Question:** Compare FastAPI with Express.js. Show how to build a REST API with automatic validation, serialization, and OpenAPI documentation using Pydantic models.

## 🔴 Advanced

### 1. GIL & Multiprocessing

**Question:** What is the Global Interpreter Lock (GIL)? How does it affect CPU-bound vs I/O-bound code? When do you need `multiprocessing` vs `threading` vs `asyncio`?

### 2. Metaprogramming

**Question:** Explain metaclasses, `__getattr__`/`__setattr__`, and descriptors. When would you use these in production (e.g., ORMs, validation frameworks)?
